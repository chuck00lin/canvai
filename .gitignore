# dependencies / builds
node_modules/
dist/
build/
coverage/
.turbo/
.cache/

# env & local config
.env
.env.*
*.local

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# safety net: internal material never ships.
# The real internal folder lives OUTSIDE this repo (../canvas-hub-internal/),
# but these patterns catch anything dropped here by habit.
internal/
notes/
scratch/
*.internal.*

# pairsketch hub local state
.pairsketch/
